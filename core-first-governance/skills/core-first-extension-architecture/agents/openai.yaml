interface:
  display_name: "Core-First Extension Architecture"
  short_description: "Architect around canonical ownership and reuse"
  default_prompt: "Use $core-first-extension-architecture to find the canonical owner, classify the change, and choose the smallest reusable implementation."
policy:
  allow_implicit_invocation: false
