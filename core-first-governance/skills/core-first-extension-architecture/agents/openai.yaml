interface:
  display_name: "Core-First Extension Architecture"
  short_description: "Resolve responsibilities, ownership, roles, and reuse"
  default_prompt: "Use $core-first-extension-architecture to decompose the exact responsibility, distinguish canonical owner from current carrier/implementation role, resolve ownership before capability/interface design, keep unsupported gaps unresolved, and choose the smallest reusable change."
policy:
  allow_implicit_invocation: false
