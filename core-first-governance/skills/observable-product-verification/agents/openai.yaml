interface:
  display_name: "Observable Product Verification"
  short_description: "Verify real user/external runtime outcomes"
  default_prompt: "Use $observable-product-verification to exercise the relevant real product surface, verify the observable outcome, inspect already-acquired surrounding evidence, and report material findings or limitations."
policy:
  allow_implicit_invocation: false
