interface:
  display_name: "Independent Review"
  short_description: "Run an unbiased read-only implementation review"
  default_prompt: "Use $independent-review to independently review the current implementation and report only evidence-based material findings."
policy:
  allow_implicit_invocation: true
